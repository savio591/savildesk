/**
 * @type {import('next-seo/lib/types').NextSeoProps}
 **/
const config = {
  openGraph: {
    type: 'website',
    locale: 'pt-BR',
    site_name: 'Savil Desk - Móveis',
  },
}

export default config
